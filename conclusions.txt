1. Using custom normalization values does not seem to work well (in terms of "color").
2. Using scale=4 does not seem to work well.
3. Using variants of the `DeFlow-DPED-RWSR` (i.e., using downscaled noisy images as clean images instead of real clean images) does not seem to work.